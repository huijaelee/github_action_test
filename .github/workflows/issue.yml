on:
  issues:
    types: [opened]

jobs:
  greet:
    runs-on: ubuntu-latest
    name: Issue comment
    steps:
      - name: issue comment
        uses: kyoncy/issue-comment-actions@v1
        with:
          message: "#번호_이름 으로 branch를 생성해주세요"
